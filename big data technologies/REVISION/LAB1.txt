CREATE TABLE INDIVIDUALS (
NAME VARCHAR2(30),
PHONE VARCHAR2(20) );

CREATE OR REPLACE TYPE O_PERSON AS OBJECT (
NAME VARCHAR2(30),
PHONE VARCHAR2(20) );

CREATE TABLE PERSONS_TABLE OF O_PERSON;


CREATE OR REPLACE TYPE O_STREET AS OBJECT (
SNAME VARCHAR2(30),
SNUMBER NUMBER );

CREATE OR REPLACE TYPE O_ADDRESS AS OBJECT (
STREET_AND_NUMBER O_STREET,
CITY VARCHAR2(30),
POSTAL_CODE VARCHAR2(8)
);

CREATE TABLE STAFF(
ID 				INTEGER 			PRIMARY KEY,
NAME 			VARCHAR2(30) 		NOT NULL,
HOME_ADDRESS 	O_ADDRESS 			NOT NULL
);


CREATE TYPE O_JOB AS OBJECT( 
JOB_TITLE VARCHAR2(20),
JOB_ID INTEGER,
SALARY_AMOUNT INTEGER,
YRS_OF_EXP INTEGER);

CREATE TABLE JOB_TABLE OF O_JOB;

INSERT INTO STAFF VALUES(1, 'NAME', O_ADDRESS(O_STREET('NAME', 111), 'CITY', 'POSTCODE'));
COMMIT;

 DROP TYPE O_PERSON;
 DROP TYPE O_ADDRESS;
 DROP TABLE STAFF;
 DROP TABLE PERSONS_TABLE;
 
 
CREATE OR REPLACE TYPE O_STREET AS OBJECT(
STREET_NAME  VARCHAR2(30),
STREET_NUMBER  NUMBER,
FLAT_NUMBER VARCHAR2(30));

CREATE OR REPLACE TYPE O_ADDRESS AS OBJECT (
STREET_AND_NUMBER O_STREET,
CITY VARCHAR2(30),
POSTAL_CODE VARCHAR2(8),
PROVINCE  VARCHAR2(20),
COUNTRY VARCHAR2(30)
);
 
CREATE OR REPLACE TYPE O_NAME AS OBJECT(
FNAME VARCHAR2(20),
MIDDLEINITIAL VARCHAR2(2),
LNAME VARCHAR2(20));

CREATE OR REPLACE TYPE O_PHONE AS OBJECT(
BUSINESS_NUM VARCHAR2(20),
HOME_NUM VARCHAR2(20),
MOBILE_NUM VARCHAR2(20));

CREATE OR REPLACE TYPE O_PERSON AS OBJECT (
NAME O_NAME,
PHONE O_PHONE,
STREET_AND_NUMBER O_ADDRESS 
);
 
CREATE TABLE STUDENT_TABLE O_PERSON;

INSERT INTO STUDENT_TABLE VALUES(O_PERSON(O_NAME('FNAME', 'MIDDELENAME', 'LNAME'), 
O_PHONE('BUSINESS_NUM', 'HOME_NUM', 'MOBILE_NUM'), 
O_ADDRESS(O_STREET('STREET_NAME', 'STREET_NUMBER', 'FLAT_NUMBER'), 'CITY', 'POSTCODE', 'PROVINCE', 'COUNTRY')
));

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 